[pytest]
pythonpath = .
testpaths = tests
addopts = --cov=app --cov-branch --cov-report=xml --cov-report=term --ignore=tests/integration
markers =
    integration: teste de integracao contra um Postgres real (nao roda por padrao, veja tests/integration)
