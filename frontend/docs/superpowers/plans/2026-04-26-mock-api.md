# Mock API Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Criar um `HttpInterceptorFn` Angular que intercepta os 5 endpoints do backend e devolve dados mockados, permitindo que o frontend rode sem o backend para validar visual e imagens.

**Architecture:** Dois arquivos novos em `src/mocks/` (`mock-data.ts` com 10 filmes e respostas fixas, `mock-interceptor.ts` com a lógica de intercepção). Uma linha adicionada em `src/app/app.config.ts` para ativar o interceptor.

**Tech Stack:** Angular 20, `HttpInterceptorFn`, `HttpResponse`, `of()` do RxJS, Jasmine/Karma para testes.

---

## Mapa de Arquivos

| Arquivo | Ação | Responsabilidade |
|--------|------|-----------------|
| `src/mocks/mock-data.ts` | Criar | Dados estáticos: 10 filmes com posters TMDB |
| `src/mocks/mock-interceptor.ts` | Criar | `HttpInterceptorFn` que intercepta por URL |
| `src/mocks/mock-interceptor.spec.ts` | Criar | Testes dos 5 endpoints mockados |
| `src/app/app.config.ts` | Modificar | Adicionar `mockInterceptor` em `withInterceptors([...])` |

---

### Task 1: Criar `mock-data.ts` com 10 filmes

**Files:**
- Create: `src/mocks/mock-data.ts`

- [ ] **Step 1: Criar o arquivo com os dados**

Criar `src/mocks/mock-data.ts` com o seguinte conteúdo completo:

```typescript
import { FilmeModel } from '../models/filme-model';

export const MOCK_FILMES: FilmeModel[] = [
  {
    id: 1,
    titulo: 'The Dark Knight',
    genero: 'Ação',
    ano: new Date('2008-01-01'),
    preco_aluguel: 12.90,
    sinopse: 'Batman enfrenta o Coringa, um criminoso que semeia o caos em Gotham City e o força a questionar tudo o que acredita.',
    imagem_url: 'https://image.tmdb.org/t/p/w500/qJ2tW6WMUDux911r6m7haRef0WH.jpg',
    duracao_minutos: 152,
    classificacao_indicativa: '14',
    data_lancamento: new Date('2008-07-18'),
    total_copias: 10,
    diretor: 'Christopher Nolan',
    copias_disponiveis: 7,
  },
  {
    id: 2,
    titulo: 'Inception',
    genero: 'Ficção Científica',
    ano: new Date('2010-01-01'),
    preco_aluguel: 11.90,
    sinopse: 'Um ladrão que rouba segredos corporativos através de sonhos recebe a missão inversa: plantar uma ideia na mente de um CEO.',
    imagem_url: 'https://image.tmdb.org/t/p/w500/9gk7adHYeDvHkCSEqAvQNLV5Uge.jpg',
    duracao_minutos: 148,
    classificacao_indicativa: '14',
    data_lancamento: new Date('2010-07-16'),
    total_copias: 8,
    diretor: 'Christopher Nolan',
    copias_disponiveis: 5,
  },
  {
    id: 3,
    titulo: 'Interstellar',
    genero: 'Ficção Científica',
    ano: new Date('2014-01-01'),
    preco_aluguel: 11.90,
    sinopse: 'Um grupo de astronautas viaja por um buraco de minhoca em busca de um novo lar para a humanidade.',
    imagem_url: 'https://image.tmdb.org/t/p/w500/gEU2QniE6E77NI6lCU6MxlNBvIx.jpg',
    duracao_minutos: 169,
    classificacao_indicativa: '12',
    data_lancamento: new Date('2014-11-07'),
    total_copias: 9,
    diretor: 'Christopher Nolan',
    copias_disponiveis: 4,
  },
  {
    id: 4,
    titulo: 'The Shawshank Redemption',
    genero: 'Drama',
    ano: new Date('1994-01-01'),
    preco_aluguel: 9.90,
    sinopse: 'Dois homens presos criam laços ao longo de anos, encontrando consolo e redenção através de atos de decência comum.',
    imagem_url: 'https://image.tmdb.org/t/p/w500/q6y0Go1tsGEsmtFryDOJo3dEmqu.jpg',
    duracao_minutos: 142,
    classificacao_indicativa: '16',
    data_lancamento: new Date('1994-09-23'),
    total_copias: 12,
    diretor: 'Frank Darabont',
    copias_disponiveis: 9,
  },
  {
    id: 5,
    titulo: 'Pulp Fiction',
    genero: 'Drama',
    ano: new Date('1994-01-01'),
    preco_aluguel: 10.90,
    sinopse: 'As vidas de dois assassinos da máfia, um boxeador, um gângster e sua esposa se entrelaçam em quatro histórias de violência e redenção.',
    imagem_url: 'https://image.tmdb.org/t/p/w500/d5iIlFn5s0ImszYzBPb8JPIfbXD.jpg',
    duracao_minutos: 154,
    classificacao_indicativa: '18',
    data_lancamento: new Date('1994-10-14'),
    total_copias: 10,
    diretor: 'Quentin Tarantino',
    copias_disponiveis: 6,
  },
  {
    id: 6,
    titulo: 'The Matrix',
    genero: 'Ficção Científica',
    ano: new Date('1999-01-01'),
    preco_aluguel: 10.90,
    sinopse: 'Um hacker descobre que a realidade é uma simulação criada por máquinas e se junta à resistência para libertar a humanidade.',
    imagem_url: 'https://image.tmdb.org/t/p/w500/f89U3ADr1oiB1s9GkdPOEpXUk5H.jpg',
    duracao_minutos: 136,
    classificacao_indicativa: '14',
    data_lancamento: new Date('1999-03-31'),
    total_copias: 11,
    diretor: 'Lana e Lilly Wachowski',
    copias_disponiveis: 8,
  },
  {
    id: 7,
    titulo: 'Forrest Gump',
    genero: 'Drama',
    ano: new Date('1994-01-01'),
    preco_aluguel: 9.90,
    sinopse: 'A vida extraordinária de um homem simples do Alabama que testemunha e participa dos maiores eventos históricos americanos.',
    imagem_url: 'https://image.tmdb.org/t/p/w500/arw2vcBveWOVZr6pxd9XTd1TdQa.jpg',
    duracao_minutos: 142,
    classificacao_indicativa: '12',
    data_lancamento: new Date('1994-07-06'),
    total_copias: 13,
    diretor: 'Robert Zemeckis',
    copias_disponiveis: 10,
  },
  {
    id: 8,
    titulo: 'O Silêncio dos Inocentes',
    genero: 'Terror',
    ano: new Date('1991-01-01'),
    preco_aluguel: 9.90,
    sinopse: 'Uma jovem agente do FBI busca a ajuda de um serial killer encarcerado para capturar outro assassino em série.',
    imagem_url: 'https://image.tmdb.org/t/p/w500/uS9m8OBk1A8eM9I042bx8XXpqAq.jpg',
    duracao_minutos: 118,
    classificacao_indicativa: '18',
    data_lancamento: new Date('1991-02-14'),
    total_copias: 7,
    diretor: 'Jonathan Demme',
    copias_disponiveis: 5,
  },
  {
    id: 9,
    titulo: 'Goodfellas',
    genero: 'Ação',
    ano: new Date('1990-01-01'),
    preco_aluguel: 10.90,
    sinopse: 'A história de ascensão e queda de Henry Hill, membro da máfia de Nova York por quase três décadas.',
    imagem_url: 'https://image.tmdb.org/t/p/w500/6QMSLvU5ziIL2T6VrkaZnZoSbnb.jpg',
    duracao_minutos: 146,
    classificacao_indicativa: '18',
    data_lancamento: new Date('1990-09-19'),
    total_copias: 8,
    diretor: 'Martin Scorsese',
    copias_disponiveis: 3,
  },
  {
    id: 10,
    titulo: 'Clube da Luta',
    genero: 'Drama',
    ano: new Date('1999-01-01'),
    preco_aluguel: 10.90,
    sinopse: 'Um insone e um vendedor de sabonetes fundam um clube de luta clandestino que evolui para algo muito mais perigoso.',
    imagem_url: 'https://image.tmdb.org/t/p/w500/pB8BM7pdSp6B6Ih7QZ4DrQ3PmJK.jpg',
    duracao_minutos: 139,
    classificacao_indicativa: '18',
    data_lancamento: new Date('1999-10-15'),
    total_copias: 9,
    diretor: 'David Fincher',
    copias_disponiveis: 6,
  },
];
```

- [ ] **Step 2: Confirmar que o arquivo compila**

Rodar:
```bash
npx tsc --noEmit
```
Resultado esperado: sem erros de tipo.

- [ ] **Step 3: Commit**

```bash
git add src/mocks/mock-data.ts
git commit -m "feat: add mock film data with TMDB poster images"
```

---

### Task 2: Criar `mock-interceptor.spec.ts` e `mock-interceptor.ts` (TDD)

**Files:**
- Create: `src/mocks/mock-interceptor.spec.ts`
- Create: `src/mocks/mock-interceptor.ts`

- [ ] **Step 1: Escrever os testes (arquivo spec)**

Criar `src/mocks/mock-interceptor.spec.ts`:

```typescript
import { TestBed } from '@angular/core/testing';
import {
  HttpClient,
  provideHttpClient,
  withInterceptors,
} from '@angular/common/http';
import {
  HttpTestingController,
  provideHttpClientTesting,
} from '@angular/common/http/testing';
import { mockInterceptor } from './mock-interceptor';
import { MOCK_FILMES } from './mock-data';

describe('mockInterceptor', () => {
  let http: HttpClient;
  let httpTesting: HttpTestingController;

  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [
        provideHttpClient(withInterceptors([mockInterceptor])),
        provideHttpClientTesting(),
      ],
    });
    http = TestBed.inject(HttpClient);
    httpTesting = TestBed.inject(HttpTestingController);
  });

  afterEach(() => httpTesting.verify());

  it('POST /users/login retorna token mock', (done) => {
    http.post<{ token: string }>('/gateway/user/api/v1/users/login', {}).subscribe((res) => {
      expect(res.token).toBe('mock-jwt-token');
      done();
    });
    httpTesting.expectNone('/gateway/user/api/v1/users/login');
  });

  it('POST /users/ retorna 200', (done) => {
    http.post('/gateway/user/api/v1/users/', {}).subscribe((res) => {
      expect(res).toBeDefined();
      done();
    });
    httpTesting.expectNone('/gateway/user/api/v1/users/');
  });

  it('GET /filmes/ retorna array com 10 filmes', (done) => {
    http.get<any[]>('/gateway/movie/v1/filmes/').subscribe((res) => {
      expect(res.length).toBe(10);
      expect(res[0].titulo).toBe('The Dark Knight');
      done();
    });
    httpTesting.expectNone('/gateway/movie/v1/filmes/');
  });

  it('GET /filmes/2 retorna o filme com id 2', (done) => {
    http.get<any>('/gateway/movie/v1/filmes/2').subscribe((res) => {
      expect(res.id).toBe(2);
      expect(res.titulo).toBe('Inception');
      done();
    });
    httpTesting.expectNone('/gateway/movie/v1/filmes/2');
  });

  it('GET /filmes/99 retorna o primeiro filme quando id nao existe', (done) => {
    http.get<any>('/gateway/movie/v1/filmes/99').subscribe((res) => {
      expect(res.id).toBe(MOCK_FILMES[0].id);
      done();
    });
    httpTesting.expectNone('/gateway/movie/v1/filmes/99');
  });

  it('POST /alugueis/ retorna aluguel com data de devolucao e pagamento', (done) => {
    http.post<any>('/gateway/rent/v1/alugueis/', { filme_id: 1 }).subscribe((res) => {
      expect(res.aluguel.data_prevista_devolucao).toBeDefined();
      expect(res.pagamento.aluguel_id).toBe('MOCK-001');
      expect(res.pagamento.amount).toBe(9.99);
      done();
    });
    httpTesting.expectNone('/gateway/rent/v1/alugueis/');
  });
});
```

- [ ] **Step 2: Rodar os testes para confirmar que FALHAM**

```bash
npx ng test --include="src/mocks/mock-interceptor.spec.ts" --watch=false --browsers=ChromeHeadless
```
Resultado esperado: erros de `Cannot find module './mock-interceptor'`.

- [ ] **Step 3: Criar `mock-interceptor.ts`**

Criar `src/mocks/mock-interceptor.ts`:

```typescript
import { HttpInterceptorFn, HttpResponse } from '@angular/common/http';
import { of } from 'rxjs';
import { MOCK_FILMES } from './mock-data';

export const mockInterceptor: HttpInterceptorFn = (req, next) => {
  const url = req.url;

  if (req.method === 'POST' && url.includes('/users/login'))
    return of(new HttpResponse({ status: 200, body: { token: 'mock-jwt-token' } }));

  if (req.method === 'POST' && url.includes('/users/'))
    return of(new HttpResponse({ status: 200, body: {} }));

  if (req.method === 'GET' && url.includes('/filmes/')) {
    const idMatch = url.match(/\/filmes\/(\d+)/);
    if (idMatch) {
      const filme = MOCK_FILMES.find(f => f.id === +idMatch[1]) ?? MOCK_FILMES[0];
      return of(new HttpResponse({ status: 200, body: filme }));
    }
    return of(new HttpResponse({ status: 200, body: MOCK_FILMES }));
  }

  if (req.method === 'POST' && url.includes('/alugueis/')) {
    const devolucao = new Date();
    devolucao.setDate(devolucao.getDate() + 7);
    return of(new HttpResponse({
      status: 200,
      body: {
        aluguel: { data_prevista_devolucao: devolucao.toISOString() },
        pagamento: { aluguel_id: 'MOCK-001', amount: 9.99 },
      },
    }));
  }

  return next(req);
};
```

- [ ] **Step 4: Rodar os testes para confirmar que PASSAM**

```bash
npx ng test --include="src/mocks/mock-interceptor.spec.ts" --watch=false --browsers=ChromeHeadless
```
Resultado esperado: `6 specs, 0 failures`.

- [ ] **Step 5: Commit**

```bash
git add src/mocks/mock-interceptor.ts src/mocks/mock-interceptor.spec.ts
git commit -m "feat: add mock HTTP interceptor for all 5 API endpoints"
```

---

### Task 3: Ativar o mock interceptor em `app.config.ts`

**Files:**
- Modify: `src/app/app.config.ts`

- [ ] **Step 1: Adicionar o `mockInterceptor` em `withInterceptors`**

Estado atual de `src/app/app.config.ts`:
```typescript
provideHttpClient(withInterceptors([authInterceptor]))
```

Alterar para:
```typescript
provideHttpClient(withInterceptors([mockInterceptor, authInterceptor]))
```

E adicionar o import no topo do arquivo:
```typescript
import { mockInterceptor } from '../mocks/mock-interceptor';
```

O arquivo final ficará:
```typescript
import { ApplicationConfig, provideBrowserGlobalErrorListeners, provideZoneChangeDetection } from '@angular/core';
import { provideRouter, withRouterConfig } from '@angular/router';
import { routes } from './app.routes';
import { HTTP_INTERCEPTORS, provideHttpClient, withInterceptors } from '@angular/common/http';
import { authInterceptor } from '../security/http-interceptor';
import { mockInterceptor } from '../mocks/mock-interceptor';

// @ts-ignore
export const appConfig: ApplicationConfig = {
  providers: [
    provideBrowserGlobalErrorListeners(),
    provideZoneChangeDetection({ eventCoalescing: true }),
    provideRouter(
      routes,
      withRouterConfig({
        onSameUrlNavigation: 'reload',
        paramsInheritanceStrategy: 'always',
      })
    ),
    provideHttpClient(withInterceptors([mockInterceptor, authInterceptor])),
  ],
};
```

- [ ] **Step 2: Rodar o build para confirmar que compila**

```bash
npx tsc --noEmit
```
Resultado esperado: sem erros.

- [ ] **Step 3: Verificar no browser**

Com o servidor já rodando (`npm start`), abrir http://localhost:4200 e confirmar:
- Dashboard exibe 10 filmes com posters
- Clicar em um filme abre a página de detalhe
- Botão de alugar retorna mensagem de sucesso
- Login com qualquer email/senha funciona

- [ ] **Step 4: Commit**

```bash
git add src/app/app.config.ts
git commit -m "feat: activate mock interceptor for frontend-only development"
```
